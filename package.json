{
  "name": "threadbound",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Persistent cooperative roguelite RPG integrated with Threaded through OAuth2 + versioned APIs.",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/application/GameService.js && node --check src/infrastructure/SQLiteGameRepository.js",
    "test": "node --test test/*.test.js test/**/*.test.js",
    "test:e2e": "playwright test"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "express-session": "^1.18.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
