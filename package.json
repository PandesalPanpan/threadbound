{
  "name": "threadbound",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Separate game application integrated with Threaded through OAuth2 + versioned APIs.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "express-session": "^1.18.1"
  }
}
